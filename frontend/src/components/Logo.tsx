export const Logo = ({ className = '' }: { className?: string }) => (
    <div className={`flex items-center justify-center gap-2 text-gray-900 text-xl font-bold ${className}`}>
      Sistema de Gestão Integrada(SGI)
    </div>
  );
  